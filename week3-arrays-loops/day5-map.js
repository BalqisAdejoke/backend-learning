let numbers = [1, 2, 3, 4];
let doubledNumbers = numbers.map(function (number) {
    return number * 2; 
});

console.log(doubledNumbers);

let scores = [10 ,20 ,30, 40];
let multipleScores = scores.map(function (scores) {
    return scores * 3;
});

console.log(multipleScores);
